import {shallowMount} from "@vue/test-utils";
import {expect} from 'chai';
import PostIndex from '@/views/post/Index';

describe('PostIndex', () => {

    // it('renders the markup correctly', () => {
    //     let wrapper = shallowMount(PostIndex);
    //     expect(wrapper).to.match.snapshotItem();
    // });
});
